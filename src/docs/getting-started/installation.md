---
title: Installing Nimbus
section: Getting started
---
# Installing Nimbus

![Nimbus logo](./images/logo.png)

Install the CLI with your package manager:

```bash
npm install @nimbus/cli
```

The package moved to a scope in version 2. The old `nimbus` package is no longer
maintained, so update your install command.
