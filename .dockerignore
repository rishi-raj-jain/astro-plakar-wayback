# Keep the build context small. src/docs is the starting docs baked into the
# image, so it must stay in the context (its large shared media is the exception:
# `npm run seed` regenerates src/docs/media in the builder from seed/shared).
node_modules
dist
.astro
.plakar
src/docs/media
.git
.github
.playwright-mcp
*.log
npm-debug.log*
.DS_Store
.vscode
fly.toml
